<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiangying.mapper.AuthPermissionMapper">

    <select id="selectPermissionKeyByUserId" resultType="java.lang.String">
        select p.permission_key
        from auth_user_role ur
                 left join auth_role r on ur.role_id = r.id
                 left join auth_role_permission rp on ur.role_id = rp.role_id
                 left join auth_permission p on rp.permission_id = p.id
        where ur.user_id = #{id}
    </select>
</mapper> 